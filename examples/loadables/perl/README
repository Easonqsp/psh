This illustrates how to build a perl interpreter into psh.  It's not
especially useful; more a proof of concept (it provides none of the
psh internals to the perl interpreter, for example).

This *may* require adding "-rpath /path/to/perl/CORE" and -lperl options
when compiling psh itself.
